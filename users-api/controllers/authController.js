class Auth {
  login = async (req, res) => {
    // Gonna take the fingerprint
    // Gonna make a request to the python to verify if it's valid
    // If successful, we then come back and generate a token
  }
}

module.exports = new Auth()
